{
  "name": "silencelaboratories",
  "private": true,
  "workspaces": [
    "packages/**/*"
  ],
    "scripts": {
      "format": "npm run format --workspaces",
      "lint": "npm run lint --workspaces",
      "format:lint": "npm run format && npm run lint"
    }
  
}
